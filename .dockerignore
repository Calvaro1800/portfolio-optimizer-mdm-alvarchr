# Ignore the .env file with sensitive credentials (secrets)
.env

# Ignore Python bytecode files
*.pyc
*.pyo
*.pyd

# Ignore virtual environments
.venv/
venv/

# Ignore logs
logs/

# Ignore testing files and directories
tests/

echo "models/llama/*.gguf" >> .gitignore
git add .gitignore
git commit -m "🚫 Prevent heavy models from being tracked again"
git push origin main
